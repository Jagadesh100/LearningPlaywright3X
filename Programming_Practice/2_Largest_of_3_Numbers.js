let a = 12,
  b = 8,
  c = 20;
if (a > b && a > c) {
  console.log(`${a} is gretaer than ${b} and ${c}`);
} else if (b > c) {
  console.log(`${b} is gretaer than ${a} and ${c}`);
} else {
  console.log(`${c} is gretaer than ${b} and ${a}`);
}
