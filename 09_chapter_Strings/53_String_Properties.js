// length properties gives the length of the string
// includes whitespaces too

let str1 = "TVK CM";
console.log(`Length of ${str1} is ${str1.length}`);

let str2 = `Hello|
Are You ready for interview?
how many years experience do you have?
`; // Here it counts even the newline

console.log(`Length of ${str2} is ${str2.length}`);
