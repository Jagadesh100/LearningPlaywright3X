let warehouseValue = null;
let dashboardValue = warehouseValue ?? 3.25; // if refrenced value is null, ?? Null Coleacing Opertaor replaces value present in RHS to variable
console.log(dashboardValue);
